# env file
.env

# OS / editor
**/.DS_Store

# dev tooling — /bin/ holds locally-built tools, never committed
/bin/
/CLAUDE.local.md
/.flow/
/.claude/settings.local.json
/.workspace/
/.home/
/.mcp.json
